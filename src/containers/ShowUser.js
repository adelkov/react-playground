import {connect} from 'react-redux';
import UserDetail from "../components/views/UserPage/UserDetail";

function mapStateToProps() {
    return {};
}

export default connect(
    mapStateToProps,
)(UserDetail);
